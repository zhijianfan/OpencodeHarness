# Task T1 — End-to-End, Security, and Scale Verification

You are worker T1 of 14 in the CtxPack parallel implementation run, in `D:\OpencodeDev`
(`feature/CyberMaster`, base commit `2d913472a`). IMPLEMENT ONLY THIS TASK. You own
verification EVIDENCE, not production behavior — failures you cannot fix are filed back
to the owning lanes in your HANDOFF, never patched around.

## Hard rules

- Edit ONLY the files under OWNED FILES (all NEW). No production files. No git writes.
  No installs. No repo-root tests.
- You may READ anything (you are the verifier), but you may not modify it.
- Bun: `BUN="$HOME/.bun-npm/node_modules/@oven/bun-windows-x64/bin/bun.exe"`.
- You run AFTER M1 integration (all lanes landed + SDK mirrored). Run against the
  integrated tree.

## Owned files (CREATE)

`packages/core/test/ctxpack-acceptance.test.ts`,
`packages/app/src/pages/canvas/ctxpack-runtime-observability.test.ts`,
`packages/app/e2e/ctxpack.spec.ts` (playwright spec — authored, runs in CI),
`docs/ctxpack-verification.md`, `devplan/ctxpack/HANDOFF-T1.md`.

## Verification scope

1. **Core acceptance** (`ctxpack-acceptance.test.ts`, real repo + capability service +
   capsule store + event recorder): create single + multi-block pack; search title/
   keyword/fragment; filters intersect; all seven sorts cursor-stable (two pages, no
   dupes); materialize returns capsule ref without text; snapshot admission atomicity
   (denied attachment → no input row); source independence after admission (delete pack,
   snapshot unchanged); usage increments once per admission; cross-workspace fragment
   denied; secret source denied (schema); 9 attachments rejected; 6,001 tokens rejected;
   includeDeleted semantics; restore after delete; revision conflicts.
2. **Runtime observability** (`ctxpack-runtime-observability.test.ts`, app package test):
   R1 adapter + U2 view against fake SDK/router: stale projection preserved on transient
   failure; reconnect refetch; burst coalescing; no polling (no setInterval/EventSource/
   WebSocket — grep the adapter source); permission-denied explicit state; layout purity
   (adapter writes only `{ query }` to local view — assert written object keys contain no
   items/selected/content).
3. **E2E spec** (`packages/app/e2e/ctxpack.spec.ts` — authored for `playwright test`,
   tagged with `test.describe("ctxpack")`): select text in a chat block → Save as new
   CtxPack → dialog defaults → save → open CtxPackBrowser → detail shows fragments in
   order → drag icon sets frozen MIME → drop on v2 composer → chip appears → send →
   network asserts exactly ONE `session.prompt` and the request carries
   `contextAttachments` (capsule refs, no text) → failure rollback keeps draft. Mark
   scenarios that require a running host as `test.skip` with a comment when the harness
   in CI doesn't have one — the spec must be runnable in CI with env-driven skips.
4. **Redaction sweep**: sentinel `CTXPACK_SECRET_SENTINEL_7812` in title/keyword/fragment;
   grep the CtxPack diff for every log/event/drag/diagnostic path —
   `rg -n "console\.(log|debug|info)|Error\(|message:" devplan/ctxpack packages/core/src/ctxpack packages/core/src/capability packages/core/src/context-broker packages/app/src/context/ctxpack packages/app/src/pages/canvas/blocks/ctxpack-browser packages/app/src/components/prompt-input | head` — assert no
   content interpolation.
5. **Prohibited-pattern scan** (run and record results):
   `rg -n "/api/block-runtime/event|chatgpt\.com/backend-api/conversation|setInterval.*(ctxpack|context)|createMockChatRelayContext" packages` — CtxPack diff must introduce none.
6. **10k-pack behavior** (core-level test): seed 10,000 packs × 2 fragments via direct
   repo inserts (bounded time — skip if > 60s, record timing); assert page limit ≤ 50,
   cursor pagination no-dupes across 3 pages, FTS query hits the virtual table
   (`EXPLAIN QUERY PLAN ctx_pack_fts` contains `VIRTUAL TABLE INDEX`), detail fetched
   only on open (repo.get not called during list).

## Deliverable `docs/ctxpack-verification.md`

Include: commit tested, runtime flag values (CYBERMASTER_BLOCK_RUNTIME_V2 present in
`packages/core/src/flag/flag.ts`), migration result (fresh DB), every test command +
exit code + pass/fail, e2e scenario list + how to run, network transport count, redaction
result, 10k measurements, known limitations (plan §10), and everything NOT verifiable
locally (bun-gated generate, full browser e2e) with the exact CI commands.

## Gates

```bash
cd packages/core && $BUN test --only-failures test/ctxpack-acceptance.test.ts
cd packages/app && $BUN test --conditions=solid --preload ./happydom.ts src/pages/canvas/ctxpack-runtime-observability.test.ts
```

Record results verbatim in HANDOFF.

## HANDOFF (`devplan/ctxpack/HANDOFF-T1.md`)

Format: Base commit / ## Files changed / ## Tests / ## Public exports / ## Central
integration actions / ## Assumptions / ## Known limitations / ## Prohibited-pattern scan
+ a **## Lane failures** section routing every failing check to its owning lane
(S/C/X/P/U1-U5/R/Q/C2/M).

Reply with: files changed, what was verified, what failed and where it routes.
