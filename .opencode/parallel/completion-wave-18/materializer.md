# Worker — authorized CtxPack materialization

Model opencode-go/deepseek-v4.1-flash/high. Own ONLY modular/packages/adapters-opencode/src/ctxpack-materializer.ts and test/ctxpack-materializer.test.ts. No exploration, commands/tests, Git, delegation, native/fork imports, other edits. Attached snapshots supply complete context. Produce code promptly and report uncertainties; master verifies after return.

Frozen types in @cybermastery/contracts/ctxpack-capsule (attached): ContextBudget, DefaultInteractiveContextBudget, StoredCapsule, CtxPackAttachment, CtxPackMaterializeRequest/Result/Error, CtxPackSnapshot; @cybermastery/contracts/ctxpack: CtxPackActor, CtxPackInfo, CtxPackError. Actual catalog CtxPackCatalog and store CtxPackCapsuleStore from attached modules. Export makeCtxPackMaterializer(options:{readonly catalog:CtxPackCatalog;readonly capsules:CtxPackCapsuleStore;readonly authorize:(input:{readonly actor:CtxPackActor;readonly operation:"ctxpack.materialize"|"chat.context.attach"|"ctxpack.read";readonly pack?:CtxPackInfo;readonly target?:{readonly workspaceID:string;readonly instanceID:string;readonly functionalityID:string}})=>Effect.Effect<void,CtxPackMaterializeError>;readonly now?:()=>number;readonly diagnostics?:(entry:{readonly operation:"materialize"|"snapshot";readonly workspaceID:string;readonly attachmentCount:number;readonly byteLength:number;readonly estimatedTokens:number})=>Effect.Effect<void>}). Return {materialize:(actor:CtxPackActor,request:CtxPackMaterializeRequest)=>Effect.Effect<CtxPackMaterializeResult,CtxPackMaterializeError>;snapshotForSessionInput:(input:{readonly actor:CtxPackActor;readonly targetInstanceID:string;readonly targetFunctionalityID:string;readonly attachments:readonly CtxPackAttachment[];readonly budget:ContextBudget})=>Effect.Effect<CtxPackSnapshot,CtxPackMaterializeError>}. Constructor synchronous or Effect-based accepted but methods should need no additional environment.

Port CUSTOM baseline `ctxpack/materialize.ts:215–477`, not a native engine: materialize reads catalog.get(includeDeleted=true), requires pack operation + chat.context.attach for target, deny-first private policy is already catalog.get, checks deleted and exact expected contentHash, builds StoredCapsule with `ctxkpsl_` ID from native ascending; purpose exactly 'ctxpack-attachment', audience exactly [targetFunctionalityID], createdBy user/instance, summary/title; facts keys ctxpack.id, ctxpack.fragmentCount and tags if present, sensitivity+sourceRef; references include a pack reference (label/hash/sensitivity) then each fragment `ctxpack.fragment` with fragment id, label/source, summary:text, hash/sensitivity. Budget DefaultInteractiveContextBudget; expiresAt absent. store immutable via actual capsule service; storage failures sanitize defects without bodies. Return only IDs/title/tags/hash/token estimate. Diagnostics counts/bytes only, no private text.

snapshotForSessionInput: require target capability first (including empty attachment), then duplicate capsule IDs reject before count>8, max8. Read each workspace-scoped capsule, verify workspace, purpose, creator instance, target audience and expiry <= now, then read CURRENT pack by attachment.source.ctxPackID(includeDeleted true). Require ctxpack.read with actual pack, check deleted and capsule/attachment/pack contentHash all equal (fail with CtxPackContentChanged currentContentHash) BEFORE materializing source texts. Resolve fragment ids in capsule.references in order against CURRENT pack.fragments; missing fragment rejects, no fallback to caller body. Derive tags from capsule's ctxpack.tags fact (not current title tags); validate allowed ParallelPlan, reject corrupt facts. Return deep-frozen snapshot V1 with original capsule ID, attachment.label, current source pack ID/hash, ordered fragments with text/source/hash. Compute bytes and tokens of JSON.stringify({version:1,attachments,createdAt}) exactly; check bytes before tokens against supplied budget and return typed over-budget error without content. Empty snapshot byte/tokens=0 (baseline). Detach all caller inputs BEFORE first await (including actor/attachments/budget); revalidate mutable actor/pack authorization at store/return boundary if needed. No usage counter/event: usage is admitted-only separate lane. Do not cache current pack content for retries: session facade will freeze on first admission and reuse persisted snapshot on exact retry.

Tests with real file native DB/capsule/catalog and source fixtures: ensure actual pack/fragment hash, tagged output, recipient confidentiality, creator-private deny, actor/target/purpose/workspace/expiry mismatch, attachment identity collisions, empty/max8/9 count, precise UTF8 budget and tag facts, pack removal/hash tamper and corruption fail closed without private text in errors, diagnostics redaction, usage remains zero after materialize/snapshot; no mocks/globalThis/sleeps. Existing baseline tests and completed catalog/capsule code attached. Keep Effect v4 Service methods bound as named vars, no any/star/aliased imports/unchecked casts.
