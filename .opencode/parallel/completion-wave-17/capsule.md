# Worker1/2 — durable CtxPack capsule storage

Model opencode-go/deepseek-v4.1-flash/high. Own ONLY modular/packages/adapters-opencode/src/ctxpack-capsule.ts and test/ctxpack-capsule.test.ts. All context supplied in attachments. No exploration, commands/tests, Git, delegation or edits outside owned files. Implement, then report uncertainties; master tests after both workers.

Frozen contract from @cybermastery/contracts/ctxpack-capsule (master-owned): ContextBudget, DefaultInteractiveContextBudget, StoredCapsule, CapsuleResult, CapsuleError; signatures visible in attached source. Export makeCtxPackCapsuleStore(options?:{readonly now?:()=>number}): Effect constructor capturing Database.Service, returning {store:(capsule:StoredCapsule)=>Effect.Effect<StoredCapsule,CapsuleError>;get:(workspaceID:string,capsuleID:string)=>Effect.Effect<StoredCapsule|undefined,CapsuleError>;materialize:(input:{workspaceID:string;capsuleID:string;requestedRefs:readonly string[];budget:ContextBudget})=>Effect.Effect<CapsuleResult,CapsuleError>}. Methods capture DB once; no further environment required. No authorization in this mechanical store: later materializer authorizes before using it.

Preserve baseline `context-broker/{sql,capsule}.ts` behavior: SAME native Database.Service; owned cm_context_capsule table, cm_migration id 0008-ctxpack-capsule. Columns id/workspace_id/purpose/content_hash/capsule_json/created_by_json/budget_json/created_at/expires_at; index workspace_id,created_at DESC. The Functionality.Capsule schema is a fork-only source reference, NOT a runtime import; add a local Effect Schema for exact body fields and decode persisted JSON with Schema.UnknownFromJsonString + strict excess-property behavior. `createdBy`/budget are deliberately out of body JSON in separate columns, with their full typed shapes preserved. No `any`/unvalidated casts. Validate unknown runtime callers (even typed callers may pass invalid data) before writing, detach and persist canonical stable JSON. Require nonempty id? Baseline assigns `ctxkpsl_`+official `ascending()` when id empty; caller-supplied nonempty ID is preserved; equal second store is conflict, not an idempotent overwrite. Workspace-scoped get, corrupt row => sanitized typed `ContextCapsule.Corrupt` with IDs only; no JSON/body leak in errors/logs; no global state/independent DB. On storage failure return sanitized defects without revealing content. Both constructors/new DB and reopen path must work. Do not mutate the native migration journal or fork `context_capsule` table.

Materialize: missing/not-found, expiresAt<=now expired; requestedRefs=[] keeps all references/artifacts; otherwise keep only records with `ref.id` in requested set, plus entries without a ref id always kept. Unresolved refs keep request order/duplicates and include only ids absent from both source reference arrays. `facts` all included, recentEvents excluded from content. Compute UTF8 bytes of JSON.stringify({facts,references,artifactRefs}); estimatedTokens ceil(bytes/4). Byte budget checked before token budget. Outcome typed exact discriminant/status fields, never private content in errors. `get` and successful result detach caller/DB objects so mutating returns cannot change stored data. No arbitrary TTL added to nonexpiring capsules; missing expiresAt remains undefined.

Tests: native file DB via createMediatedFixtures/createMediatedKernel + databaseCleanup; real SQLite, immutable duplicate ID, explicit ID, preserved separate metadata, cross-workspace, read-after-reopen, corruption of each JSON column, expiry boundary deterministic injected clock, filtering/unresolved order, UTF-8 byte/tokens and bytes-vs-tokens precedence; no fake store, globalThis or sleeps. Worker allowed only owned files and no tests until master integration.
