# Worker2/2 — CtxPack admitted-use ledger

Model opencode-go/deepseek-v4.1-flash/high. Own ONLY modular/packages/adapters-opencode/src/ctxpack-usage.ts and test/ctxpack-usage.test.ts. All context attached. No exploration, commands/tests, Git, delegation or other edits. Implement promptly, report uncertainties, no test-pass claims.

Export makeCtxPackUsage(options:{readonly catalog:CtxPackCatalog;readonly authorize:(request:CtxPackAccess)=>Effect.Effect<void,CtxPackError>;readonly publish?:(event:CtxPackChanged)=>Effect.Effect<void>;readonly now?:()=>number}). Effect constructor captures native Database.Service and EventBoundary; returns {recordAdmittedUse:(input:{readonly workspaceID:string;readonly userID:string;readonly ctxPackIDs:readonly string[];readonly sessionInputID:string;readonly admittedAt:number})=>Effect.Effect<void,CtxPackError>} with captured environment. This is a separate service composing the EXISTING catalog over the SAME DB, not a second catalog/provider loop. Use only contract CtxPackActor/CtxPackAccess/CtxPackError/CtxPackChanged and catalog interface from attached source; no fork runtime imports. Caller must supply actor and verified admitted input ID explicitly.

Preserve baseline `ctxpack/usage.ts`: owned `cm_ctx_pack_usage_admission` table (ctx_pack_id,session_input_id,time_recorded PK pair), cm_migration id 0009-ctxpack-usage; DB authoritative; DISTINCT ctxPackIDs stable order. `recordAdmittedUse` runs ONE EventBoundary.transaction across all packs; uses INSERT ... ON CONFLICT DO NOTHING RETURNING to count once per pack+message; increments cm_ctx_pack.attached_count only on fresh ledger insert, last_attached_at=MAX(COALESCE(...),admittedAt); return no duplicate hints on retries. Verify in SAME transaction that native session_input.id=sessionInputID exists AND belongs to the caller's workspace via session.workspace_id; otherwise typed permission denied and no ledger/count. This is a stricter verified-admission condition than fork's generic ledger and should be explicitly documented as a selected-host security boundary. No backend data from unverified IDs. Pack must exist, be live, match workspace and private creator, authorize ctxpack.read on actual pack BEFORE any mutation and again within transaction for all packs; if one pack is invalid/changed/denied nothing commits and no hints. Do not automatically increment on materialize/recall/freeze failure. A session_input can be admitted before promoting; no provider execution is needed.

Publish workspace.ctxpack.changed with only IDs/revision/change:'used' after outer commit; no hint on duplicate use/rollback; noninterrupt publisher failure swallowed without logging text/defect, committed counts remain. No concurrency inside write. Validate/detach inputs before awaits including safe admittedAt, nonempty workspace/user/SessionInputID, <=8 unique IDs? Baseline DISTINCT permits duplicates and up to any count; use 32-pack request cap as a new bounded control only if justified, otherwise preserve baseline and no arbitrary bound. Avoid copying the 851-line repository; update counts via parameterized SQL in one native DB, get current revision for minimal event.

Tests: real native SessionV2.create + prompt(resume:false) with verified actor via PrivatePromptContext, actual catalog.create same workspace and two actor cases; real databaseCleanup/disposal. Test same input/pack twice counts once, second admitted input counts again, duplicate IDs in one request dedupe, two-pack atomic failure on denied second pack, wrong workspace/foreign Session ID/missing Session input reject, private pack other user denied, revocation between initial check and commit, outer EventBoundary rollback suppresses ledger/count/hints, publisher defect after commit doesn't roll back, two independent runtimes racing same admission count once. Reopen DB and verify ledger durable. No globalThis/mocks/sleeps; keep native event/Session authority unchanged. Master runs typechecks/tests after barrier.
