# Task 5 — Update the ChatRelay specs to the session-based architecture

You are worker 5 of 5. Implement ONLY this task.
- Edit ONLY the files listed as your owned files. Never touch other files, shared configs, lockfiles, or generated code.
- Do not wait for, check on, or communicate with other workers.
- When done, reply with: files changed, what was implemented, what was left undone or uncertain.

## Owned files
- `specs/relay/architecture.md`
- `specs/relay/oauth.md`
- `specs/relay/chat-relay-session-migration.md`

## Goal
Docs-only update. The ChatRelay no longer owns OAuth/transport/sessions. Rewrite these three
files to describe the new architecture; keep them dense and factual (the repo treats specs as
the authoritative design record).

## New architecture (write this)
- ChatRelay block = `builtin:chat-relay` canvas block holding only `{ sessionID binding, blockID, UI prefs }`.
- Binding service mirrors `builtin:master-agent` (`packages/core/src/workspace/chat-relay-session.ts`):
  `get/ensure/reset` over `/api/workspace/:workspaceID/chat-relay/:blockID(/:ensure|:reset)` (group `server.workspace.chatRelay`).
- The bound session is a normal OpenCode Session: auth = opencode's `CodexAuthPlugin`
  (`packages/opencode/src/plugin/openai/codex.ts`, client id `app_EMoamEEZ73f0CkXaXp7hrann`,
  issuer `https://auth.openai.com`), inference via the openai provider routed to
  `https://chatgpt.com/backend-api/codex/responses` (codex.ts:12) — NOT `/backend-api/conversation`.
- Frontend: `ChatRelayBody` renders `CanvasSessionSurface` from the binding (session surface owns
  composer, streaming events, auth screens, messages).
- Removed: `packages/relay/src/provider/*` (chat-relay state machine, chatgpt/oauth/sse transports),
  credentials.json/session.json/operating-context.jsonl relay storage, `/api/relay/*` endpoints
  (initialize/status/submit/dispose/payload.*), the 5s status polling and block-local message state.
- OperatingAgent context forwarding (the old operating-context.jsonl semantics) = explicit FOLLOW-UP:
  a session-event consumer, not the relay.

## Per-file
- `architecture.md`: rewrite the component/deployment view to the block→binding→Session flow;
  include a short "migration rationale" (405 = Codex OAuth token sent to the ChatGPT web
  conversation endpoint; opencode native path already routes codex auth correctly).
- `oauth.md`: replace the custom device-flow description with a pointer to `CodexAuthPlugin`
  (codex.ts) as the single OAuth implementation; note the relay previously duplicated it and that
  duplication is removed. Keep any still-true endpoint reference (issuer/device endpoints) clearly
  attributed to codex.ts.
- `chat-relay-session-migration.md`: mark track statuses (T1-T6) as done/planned based on the
  implementation; keep the Decision Points section but resolve them (model not pinned at creation;
  payload endpoints dropped; operating-context forwarding = follow-up).

## Acceptance
- No file describes the removed custom stack as current.
- `specs/relay/chat-relay-session-migration.md` reflects final status.
- No changes outside the three files.

Report: files changed, implemented, uncertain.
