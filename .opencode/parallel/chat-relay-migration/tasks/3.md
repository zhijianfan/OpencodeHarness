# Task 3 — Frontend: ChatRelayBody → session binding + CanvasSessionSurface

You are worker 3 of 5. Implement ONLY this task.
- Edit ONLY the files listed as your owned files. Never touch other files, shared configs, lockfiles, or generated code.
- Read any existing code you need first; follow the repo conventions (solid-js stores, AGENTS.md style).
- Do not run repo-wide builds, typechecks, or tests — the master integrates and tests. A quick targeted check of your own code is allowed.
- Do not wait for, check on, or communicate with other workers.
- When done, reply with: files changed, what was implemented, what was left undone or uncertain.

## Owned files
- `packages/app/src/pages/canvas/workspace.tsx` (ONLY the ChatRelayBody region and its imports)
- `packages/app/src/pages/canvas/canvas.css`

## Goal
Replace ChatRelayBody's custom relay UI (initialize/login panels, authUrl/userCode, custom
submit form, submitError banner, 5s status polling, toggleImportant) with a binding-driven
block that renders the existing session surface — the same pattern as the MasterAgent block.

Template to mirror: `packages/app/src/pages/canvas/master-agent/block.tsx` (binding state →
sessionSlot) and `packages/app/src/pages/canvas/master-agent/session-options.ts` (target
construction from sessionID/directory/workspaceID — REUSE it, do NOT edit it).

## Contract (workers 1/2 provide the SDK surface — code against these names)
`serverSDK().client.v2.workspace.chatRelay` with:
- `get({ workspaceID, blockID })` → `{ data: { status: "bound", binding: ChatRelayBinding } | { status: "unbound" } }`
- `ensure({ workspaceID, blockID })` → `{ data: ChatRelayBinding }`
- `reset({ workspaceID, blockID, chatRelayResetPayload: { expectedSessionID, expectedRevision } })` → `{ data: ChatRelayBinding }`
`ChatRelayBinding = { workspaceID, blockID, functionalityInstanceID, sessionID, directory, generation, revision }`.

## Implementation
1. Read the CURRENT `ChatRelayBody` (`packages/app/src/pages/canvas/workspace.tsx`, search for `ChatRelayBody`) and the master-agent block files listed above.
2. Rewrite `ChatRelayBody`:
   - Local state: `binding` signal (`ChatRelayBinding | undefined`) + status signal (`"uninitialized" | "loading" | "ready" | "error"`). `onMount` → `ensure({workspaceID: props.workspaceID, blockID: props.block.id})` (keep idempotent; retry button on error).
   - Keep the `networkDenied()` permission gate exactly as-is (denied panel unchanged) — the project-config webfetch/websearch permission still gates this block.
   - When bound: render `CanvasSessionSurface` (imported from `./session-surface`, same usage as `master-agent/block.tsx:135-142`) with `target` built via `createMasterAgentSessionOptions({ sessionID, directory, workspaceID }).target` (import from `./master-agent/session-options` — read-only reuse), `surfaceID={`chat-relay-${props.block.id}`}`, `focused`, `onFocus` props passed through. Do NOT pass `queueEnabled` (defaults fine) unless the block keeps a `sessionBusy` concept — simpler: omit.
   - While unbound/loading/error: render a small status panel using the existing `canvas-relay-state` CSS classes (spinner for loading; error note + Retry button calling ensure again). Do NOT add new CSS classes unless truly needed; reuse `.canvas-relay-state`, `.canvas-relay-state-title`, `.canvas-relay-state-note`, `.canvas-relay-init-button`, `.canvas-relay-state-icon`.
   - DELETE from ChatRelayBody: `initialize`, `syncStatus`, `submit`, `readRelayError`, `toggleImportant`, the 5s `createEffect` poller, `authUrl`/`userCode`/`submitError`/`submitting`/`totalMessages`/`provider` signals, `pushMessage`, and the panels for awaiting-login/initializing/error/ready/denied-uninitialized. The block no longer owns messages — the session surface does.
   - The block's `relay` field on `CanvasBlock` can stay untouched elsewhere (layout serialization keeps it as presentation-only); do not modify `recordToBlock`/`toRecords`/`load`.
3. In `packages/app/src/pages/canvas/canvas.css`: remove now-unused relay classes (`.canvas-relay-submit-error`, `.canvas-relay-auth-url`, `.canvas-relay-auth-code`, message/composer rules used only by the old body) ONLY if you verified they are unreferenced anywhere else (grep). Keep `.canvas-relay-state*` classes.

## Acceptance
- No references left to `v2.relay.` (old API) or `relay.submit`/`relay.status`/`relay.initialize` in workspace.tsx.
- ChatRelayBody renders the session surface from `v2.workspace.chatRelay.ensure`; no custom submit/login code remains.
- Compiles under the existing app test suite (`bun test --conditions=browser src/pages/canvas/master-agent.e2e.test.tsx` from packages/app) — pre-existing failures allowed, but no NEW import/type errors from your region.

Report: files changed, implemented, uncertain.
