# Task 1 — Backend ChatRelay session binding (mirror builtin:master-agent)

You are worker 1 of 5. Implement ONLY this task.
- Edit ONLY the files listed as your owned files. Never touch other files, shared configs, lockfiles, or generated code.
- Read any existing code you need first; follow the repo conventions (Effect v4 patterns, .js import suffixes, bun workspace; AGENTS.md: snake_case drizzle, no else, no `any`, dynamic imports for heavy modules).
- Do not run repo-wide builds, typechecks, or tests — the master integrates and tests. A quick targeted check of your own code is allowed.
- Do not wait for, check on, or communicate with other workers.
- When done, reply with: files changed, what was implemented, what was left undone or uncertain.

## Owned files
- `packages/schema/src/chat-relay.ts` (NEW)
- `packages/core/src/workspace/chat-relay-session.ts` (NEW)
- `packages/core/src/location-services.ts`
- `packages/protocol/src/groups/chat-relay.ts` (NEW)
- `packages/protocol/src/groups/relay.ts` (DELETE)
- `packages/server/src/handlers/chat-relay-session.ts` (NEW)
- `packages/server/src/handlers/chat-relay-session-access.ts` (NEW)
- `packages/server/src/handlers/relay.ts` (DELETE)
- `packages/server/src/routes.ts`
- `packages/opencode/src/server/routes/instance/httpapi/server.ts`
- `packages/core/src/workspace/chat-relay-payload.ts` (delete only if unreferenced after your changes — grep for `ChatRelayPayload` first)

## Goal
Replace the old ChatRelay HTTP surface with a block → Session binding, using the
builtin:master-agent implementation as the template everywhere:

- Template schema: `packages/schema/src/master-agent.ts`
- Template service: `packages/core/src/workspace/master-agent.ts`
- Template protocol group: `packages/protocol/src/groups/workspace-master-agent.ts`
- Template handler: `packages/server/src/handlers/workspace-master-agent.ts`
- Template access middleware: `packages/server/src/handlers/workspace-master-agent-access.ts`

Functionality id: `builtin:chat-relay` (already registered in
`packages/core/src/workspace/service.ts` — do NOT edit that file).

## Pinned contract (workers 1 and 2 must agree exactly)
Schema namespace `ChatRelay` in `packages/schema/src/chat-relay.ts`:
- `FunctionalityID = Schema.Literal("builtin:chat-relay")` (annotate like master-agent's)
- `Binding` — EXACTLY the same fields as `MasterAgent.Binding` (workspaceID, blockID, functionalityInstanceID, sessionID, directory, generation, revision), identifier `ChatRelay.Binding`
- `InstanceConfiguration` — mirror master-agent's (directoryBinding modes, sessionBinding owned/null), version 1
- `GetResponse` = union `{ status: "bound", binding: Binding } | { status: "unbound" }`
- `ResetPayload = { expectedSessionID: SessionSchema.ID, expectedRevision: NonNegativeInt }`
- Error classes mirroring master-agent's error set, `ChatRelay` prefix (workspace-not-found, block-not-found, wrong-functionality, instance-not-found, access-denied, conflict, stale-binding, busy)

Protocol group `packages/protocol/src/groups/chat-relay.ts`, group id `server.workspace.chatRelay`, root `/api/workspace`:
- GET `workspace.chatRelay.get` → `${root}/:workspaceID/chat-relay/:blockID` → success `ChatRelay.GetResponse`; errors: workspace-not-found, block-not-found, wrong-functionality, instance-not-found, access-denied, conflict. OpenAPI identifier `v2.workspace.chatRelay.get`, summary "Get ChatRelay binding".
- POST `workspace.chatRelay.ensure` → `${root}/:workspaceID/chat-relay/:blockID/ensure` → success `ChatRelay.Binding`; errors same minus instance-not-found. Identifier `v2.workspace.chatRelay.ensure`, summary "Ensure ChatRelay binding".
- POST `workspace.chatRelay.reset` → `${root}/:workspaceID/chat-relay/:blockID/reset` → payload `ChatRelay.ResetPayload` → success `ChatRelay.Binding`; errors add stale-binding, busy. Identifier `v2.workspace.chatRelay.reset`, summary "Reset ChatRelay binding".

Core service `ChatRelaySession` in `packages/core/src/workspace/chat-relay-session.ts`:
- Interface `{ get, ensure, reset }` mirroring master-agent semantics: verifyBlock against functionality `builtin:chat-relay`; session creation via `SessionV2.Service` with `location: { directory: AbsolutePath.make(directory), workspaceID }` — do NOT pin a model (session default); functionality instance claim/conflict handling copied from master-agent (`claimInstance`, `cleanupLosingCandidate`, BindingUpdated event — publish `ChatRelay.BindingUpdated` from your schema via the master-agent event pattern in `packages/core/src/workspace/master-agent-events.ts`; do NOT edit master-agent files — define the event in your schema file).
- Register your service node in `packages/core/src/location-services.ts` EXACTLY where/how `MasterAgentBuiltin`-adjacent global nodes are registered (grep how `MasterAgent` node is wired globally — mirror the same mechanism, e.g. makeGlobalNode + deps [Database, EventV2, Workspace, FunctionalityInstance, SessionPort…]).
- Delete `packages/core/src/workspace/chat-relay-payload.ts` ONLY IF your handler rewrite leaves it unreferenced (grep). Remove its registrations in `packages/server/src/routes.ts`.

Server handlers `packages/server/src/handlers/chat-relay-session.ts` + `-access.ts`: mirror `workspace-master-agent.ts` + `workspace-master-agent-access.ts` (group handlers + middleware), calling `ChatRelaySession.Service`.

Wiring (the "Service not found" gotcha): register the new group/handler and core service in BOTH `packages/server/src/routes.ts` (applicationServices) AND `packages/opencode/src/server/routes/instance/httpapi/server.ts` (LayerNode.group) — find where `workspaceMasterAgent` / `MasterAgent` are wired and mirror. Remove the old `server.relay` group wiring and the old `packages/server/src/handlers/relay.ts`.

Delete `packages/protocol/src/groups/relay.ts` (the old relay endpoints are gone). Grep the repo for `server.relay` / `groups/relay` imports and update them (they will be in the server wiring only — your files).

## Acceptance
- Grep: no imports of `groups/relay` or `handlers/relay` remain.
- Grep: `ChatRelaySession` service + `workspace.chatRelay` endpoints wired in both server composition files.
- The contract above is implemented verbatim (worker 2 mirrors it; drift breaks the build).

Report: files changed, implemented, uncertain.
