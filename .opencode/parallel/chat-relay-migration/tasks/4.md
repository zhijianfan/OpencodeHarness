# Task 4 — Delete the custom relay provider stack (packages/relay)

You are worker 4 of 5. Implement ONLY this task.
- Edit ONLY the files listed as your owned files. Never touch other files, shared configs, lockfiles, or generated code.
- Do not run repo-wide builds, typechecks, or tests — the master integrates and tests.
- Do not wait for, check on, or communicate with other workers.
- When done, reply with: files changed, what was implemented, what was left undone or uncertain.

## Owned files
- `packages/relay/src/provider/**` (ENTIRE directory: chat-relay.ts, chatgpt.ts, oauth.ts, provider.ts, sse.ts, runplan.ts, types.ts + any tests)
- `packages/relay/src/index.ts`
- `packages/relay/package.json`
- Any `packages/relay` test file that imports from `./provider` or `src/provider` (e.g. `src/provider/*.test.ts`, `src/core` tests referencing provider) — verify with grep first

## Goal
Remove the custom ChatGPT transport/OAuth/session stack from `@opencode-ai/relay`. The
block now binds to a normal OpenCode Session (worker 1 rewrites the server handlers, worker 3
rewrites the frontend — you do NOT touch packages/server or packages/app).

## Steps
1. Grep who imports the provider modules: `grep -rn "@opencode-ai/relay" packages --include="*.ts"` and `grep -rn "provider/" packages/relay/src packages/relay/test 2>/dev/null`. Workers 1/3 own the server/app import sites — after their changes nothing outside packages/relay should reference provider code. If you find a remaining importer INSIDE packages/relay (e.g. `src/cli.ts`), fix it by removing the feature from that importer (relay cli keeps its `core/` file-organization features).
2. Delete the `packages/relay/src/provider/` directory entirely (all files).
3. Update `packages/relay/src/index.ts`: remove re-exports of provider modules; keep `core/*` exports used by the cli (`./cli` and `./core/*` exports in package.json stay).
4. Update `packages/relay/package.json`: remove the `./provider/*` export entries; keep `"."`, `"./cli"`, `"./core/*"`. Do NOT touch dependencies.
5. Check `packages/relay/src/cli.ts` compiles against the remaining exports (read it; remove only provider-dependent code paths, e.g. any chat-provider command, and note it in your report).

## Acceptance
- `grep -rn "provider/" packages/relay/src --include="*.ts"` returns nothing (except comments you may leave referencing the removed stack — remove those too).
- `grep -rn "@opencode-ai/relay" packages --include="*.ts"` shows no import of provider modules (the `"."`/`"./cli"`/`"./core/*"` imports are fine).
- `packages/relay/package.json` exports map has no provider entries.

Report: files changed, implemented, uncertain (list anything the cli lost).
