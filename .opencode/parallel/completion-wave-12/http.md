# Worker 1 of 2 — authenticated private transfer HTTP

Model opencode-go/deepseek-v4.1-flash/high. Own ONLY src/transfer-http.ts and test/transfer-http.test.ts under D:/OpencodeHarness/modular/packages/adapters-opencode. All source context is attached. No exploration, commands/tests, Git, delegation or other edits. Produce code, then a concise report with uncertainties. Do not claim tests run.

## Frozen API

Export createTransferHttp(input:{readonly source:TransferSource;readonly receiver:Effect.Success<ReturnType<typeof makeTransferReceiver>>;readonly readiness:Effect.Success<ReturnType<typeof makeTransferReadiness>>;readonly workspaceID?:string;readonly authenticate:(request:Request)=>Promise<boolean>;readonly authorizeStart:()=>Effect.Effect<void,TransferError>;readonly run:<A,E>(effect:Effect.Effect<A,E>)=>Promise<A>}). Return {fetch:(request:Request)=>Promise<Response|undefined>}. This adapter BORROWS already constructed services and owns no runtime/spool/disposal. Use the actual attached exports, no invented native routes/server graph or imports from fork source.

## Routes

Own exact /sync prefix and /api/cybermastery/sync prefix: /start, /history, /replay; unknown paths under either prefix return404 and cannot fall through to native fallback. Other paths return undefined. POST only (405 with Allow:POST). Require a nonempty Authorization HEADER plus successful authenticate callback before reading a private body; query auth_token alone is rejected. Authorization values are interpreted by the trusted callback, not hard-coded in this module. Both native Basic and host Bearer may be accepted by that callback. Bodies need application/json. Read streamed bodies with a 1MiB bound (including dishonest/missing Content-Length); cancel the reader on overflow and return413. Never retain/log raw body, credentials, tokens or error causes in public failures. Respond Cache-Control:no-store and x-opencode-session-sync-version:1 on success.

Parse JSON with Effect Schema helpers, strict excess-property rejection by source/receiver DTO decoders. Require version exactly1; unversioned/legacy requests return409 code version (no private downgrade/fallback). Malformed JSON/unknown action or wrong types =>400 invalid. Source/receiver tagged TransferError map: forbidden403, invalid400, conflict/restart/snapshot-advanced/version409, too-large413, busy503, expired410. Always use sanitized fixed public messages, never supplied error.message/cause. Unknown thrown values=>500 with a fixed generic response.

History calls source.history(parsed). Replay calls receiver.replay(parsed). Start strictly decodes version1/action grant|revoke/workspaceID optional/topologyRevision/expiresAt/requestToken. Workspace must equal input.workspaceID (including omitted implicit-local); request never selects another workspace. Recheck authorizeStart before grant/revoke; readiness manager validates lease identity/token/TTL. Read source.required BEFORE changing the lease to avoid leaving a granted lease if the required-state query fails. Then run grant/revoke and return {version:1,acceptedRevision,expiresAt,transferRequired}; for revoke, refresh required AFTER it drains because an in-flight admission may have committed private state. LeaseConflict maps409 code conflict with a fixed message. Existing manager signatures unchanged by worker2: grant(LeaseInput), revoke(LeaseInput), withPermit(...). Do not create a second readiness manager. /steal stays an authenticated explicit unavailable/conflict route rather than implementing a silent owner steal.

## Tests

Use actual makeTransferSource/makeTransferReceiver/makeTransferReadiness and real mediated fixture + spool. Test headers-only authentication/query-token rejection before body read, both owned prefixes/no-fallthrough, method/mime/version/body limits, history capability probe, real begin/append/finalize with scope guard, grant/revoke/transferRequired with the SAME manager (permit held using Deferred barrier), malformed token/workspace scope, sanitized tagged errors and unknown failure without private leaks. Keep cleanup scoped, databaseCleanup for temp SQLite, dispose receiver/source yourself in fixture; factory must not dispose borrowed services. A function-injected auth callback is a real port; don't fake restore or globalThis. Native fixture/Bundle helpers and current protocol/source/receiver modules are attached. No any, alias/star imports, non-null assertions or unchecked casts. Effect v4: Effect.catch, Cause.findErrorOption; Fiber.interrupt returns void.
