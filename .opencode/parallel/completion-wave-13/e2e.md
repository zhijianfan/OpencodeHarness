# Worker 1 of 3 — real-network application transfer acceptance

Use opencode-go/deepseek-v4.1-flash/high. Own ONLY D:/OpencodeHarness/modular/packages/adapters-opencode/test/application-transfer.test.ts (new). All context attached. No exploration, commands/tests, Git, delegation or production edits. Return exact guarantees and uncertainties, not pass claims.

Implement meaningful end-to-end tests with TWO real createApplicationAdapter instances, separate temporary SQLite filenames, the SAME trusted native directory/project/workspace/user placement, explicit distinct host authentication tokens, and separate ephemeral Bun.serve({hostname:'127.0.0.1',port:0,fetch:app.fetch}) servers. Call via actual fetch and validated JSON; never fake projection/transfer/native services. Replacements for deterministic LLM follow application.test.ts; requests counters must prove replay itself never starts a provider. Set isolated:true to keep Global tool-output cleanup inside each temporary DB area. Dispose servers, app runtimes and files using finally/databaseCleanup. Never launch external application binaries or access user data.

Public createApplicationAdapter now owns /sync and /api/cybermastery/sync, /api/session and /api/cybermastery/session. ONE managed readiness instance is passed to both Session facade and /sync/start. Fresh Session create records replay owner=logical input.workspaceID; this is NOT execution ownership. Child owners inherit a parent's non-null replay owner. native location workspace is included only when logical workspace starts with wrk; use wrk_transfer_e2e so both native and logical IDs agree. App source/receiver trusted principal is input.userID; owner is input.workspaceID; actual native Project resolved/inserted at startup. Use the same native directory in both apps so incoming Created placement matches target; never rewrite transferred native events.

Tests must cover:
1. Fresh source Session via HTTP, clean no-lease admit-only prompt (freeze count stays0, no private rows), then grant valid readiness /sync/start and admit a tagged V2 private snapshot with explicit reference. Supply proof in BOTH headers x-opencode-session-context-topology and x-opencode-session-context-lease. Freeze returns renderContextSnapshot full context with exact reference hash (see attached readiness/input fixtures), not an arbitrary proof-only string. Use a large PUBLIC prompt (~600KiB) to force chunk pagination while the private suffix stays within interactive budget. resume:false throughout.
2. Pull /sync/history capabilityOnly:true, then data with the same snapshot token; verify no private payload in capability response. Begin target /sync/replay with target directory, source token/highWater/manifestDigest and bounded expiresAt. Append each source page with digest(canonical(page)); retry a page identically; finish using actual page count/manifestDigest. Use response.nextCursor plus original binding for continuation, not a new snapshot. Verify all response canonical bytes <=512KiB and chunk-only pages; ensure more than one page actually occurred.
3. Target native messages/history contain only public text; public native event JSON has no private fragment/tagged sidecar body. Target cm_private_input API content/hash and cm_legacy_input snapshot exactly match source; clean marker transfers and private request identity is correctly legacy-prefixed. Source and target native event count/IDs and exported /sync/history highWater match. No provider calls/wakes from replay, page append or finalize.
4. Exact clean/private HTTP prompt retries at target succeed WITHOUT an active target lease or another freeze/provider turn; changed prompt/reference contentHash conflicts. Revoke source lease; a new explicit-reference prompt fails while exact private retry still succeeds unchanged.
5. Replay same finalize returns identical receipt without duplicate events. Stop target server/dispose target app, recreate against same filename and placement with a new empty spool, then repeat old finalize: durable receipt survives. Authorization/version/body/header errors cannot expose private payloads; query auth_token alone cannot authorize /sync. Scope mismatch and wrong credentials fail without target mutation.

Use Effect+Schema for decoding typed responses, with no any/casts hiding errors. Avoid sleeps, globalThis or guessed Bun APIs. Bun.serve returns server.port/url and server.stop(true); native source/receiver captures are already assembled by app. Schema helpers and response field shapes are attached. You can factor local fixture/request/schema helpers to keep tests readable, but do not duplicate transport implementation or calculate expected values using copied implementation logic.
