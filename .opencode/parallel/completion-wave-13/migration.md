# Worker 3 of 3 — representative read-only fork Session-copy upgrade

Model opencode-go/deepseek-v4.1-flash/high. Own ONLY src/fork-session-copy.ts and test/fork-session-copy.test.ts under D:/OpencodeHarness/modular/packages/adapters-opencode. Context is attached; no exploration, commands/tests, Git, delegation or other edits. This is the EARLY REPRESENTATIVE T05/T06 copied-schema experiment, not complete product-data migration. Fail closed on unsupported source records instead of pretending all historical data migrated.

## API and authority

Export makeForkSessionCopy(policy:{readonly authorize:(scope:LegacyScope)=>Effect.Effect<void,LegacyProjectionError>}), returning {restore:(input:{readonly bytes:Uint8Array;readonly expectedDigest:string;readonly scopes:readonly LegacyScope[]})=>Effect.Effect<{readonly sessions:number;readonly inputs:number;readonly checkpoints:number},LegacyProjectionError,Database.Service|EventV2.Service|EventBoundary>}. Reuse existing makeLegacyProjection and error type/codes; no invented native replayBatch or fork runtime imports. Bytes are an explicitly supplied OFFLINE copied database image. Detach them and scopes before awaiting; enforce a 512MiB byte ceiling and <=128 unique Session scopes. expectedDigest is independently supplied SHA256 of EXACT bytes with sha256: prefix; verify before opening/querying. Authorize each scope before any source disclosure and inside the ONE target EventBoundary.transaction that restores all selected Sessions. No provider wakes.

The source bytes are read into an isolated in-memory SQLite reader, not attached as a second runtime authority. Bun's documented APIs verified by master: dynamic `const { Database } = await import('bun:sqlite')` inside a narrow async reader helper, `Database.deserialize(Buffer.from(bytes))`, `source.run('PRAGMA query_only = ON')`, `source.query<ReturnType,ParamsType>(sql).all/get(...)`, and `source.close(true)`. Dynamic Database binding must be in a separate helper scope so it does NOT shadow the imported native Database namespace in the Effect function (no aliased imports). Deserialize only the detached verified image and close/finalize in finally. This avoids file/WAL time-of-check races and NEVER changes the source copy. Caller is responsible for producing a consistent serialized offline image; tests serialize then write/read an actual temp copy file to exercise that boundary. No migration touches real user databases or tries to find them.

## Exact fork schema reference

Attached fork session/sql.ts shows columns missing from official pin: session.runtime (legacy|v2|mixed); session_input.context_snapshot_json (JSON text, may be null); session_message.model_context_json (checkpoint JSON text). Native event_sequence is aggregate_id PK, seq, owner_id. event has id/aggregate_id/seq/type/data JSON text. session_context_epoch has session_id/baseline_seq/baseline/snapshot JSON text. Preserve native Session IDs, prompt message IDs, event versions, sequence, runtime classification, frozen context/request hashes, checkpoint values and epoch. Source scope owner/workspace must match recorded placement/owner; no blanket adoption/reclassification. Require declared source tables/columns, valid schema/JSON, all selected native history present through event_sequence.seq and matching runtime metadata in lifecycle events. Missing/ambiguous classification is an explicit error, not inferred.

Read all selected history ordered by seq, source input rows and compaction-message rows and epoch. Construct actual legacy BundleV1 envelopes using legacyCanonical/legacyDigest, matching existing source SessionProjectionTransfer.export conventions (attached reference): input event identity, admitted_seq/message_id, context_snapshot_json; compaction seq/model_context_json; epoch {baseline,snapshot} and baselineSeq/sourceSeq. Always validate via validateLegacyBundle before restoring. Never regenerate frozen provider bodies or strip original tags/renderer versions. Validate clean native input prompt/delivery against its public admission as well, and ensure no projected source private row is left unaccounted for.

For this representative importer, explicitly reject source histories requiring deleted-target reconstruction or containing unsupported context slots (e.g. pending V2 or missing private marker data); do not invent deletion proofs. The existing transport validator handles deletion bundles separately. Require target project metadata already provisioned (existing legacy restore checks this), no auto-import of unrelated project/workspace/catalog tables. Import selected complete Session bundles through makeLegacyProjection.restore with independently computed detached bundle digest, and recheck authorization before outer commit. If any later Session fails, rollback all target rows and public notifications. Do not copy native engines/migrations or change source files.

## Tests

Build a small sanitized SQLite fixture using the ACTUAL named fork columns from the attached schema, populate real durable event payloads and valid V1/V2/tagged snapshots/checkpoint/epoch using attached test helpers. Serialize to a temporary copy file, close it, read bytes; verify source file hash/bytes unchanged after success AND failure. A fixture is a representative copy, not a claim to recreate every production table. Target is real createSessionRuntime/createMediatedFixtures with native migrations and extension kernel.

Cover successful private input + clean input + runtime + checkpoint + epoch preservation, reopen target then verify frozen hashes/legacy export equality and exact retry without freeze, digest mismatch before writes, wrong owner/workspace, corrupt/pending/missing private snapshot rejection, unsupported source schema/classification rejection, later aggregate failure atomic rollback with no notifications, and explicit rollback point (discard migrated target/reopen untouched source copy and prove its bytes/data still match). Native migration ledger must be unchanged by extension import. Full product migration (CtxPacks, drafts, blobs, legacy message/part stores and deletion reconstruction) remains a recorded limitation. Do not weaken tests to accept lossy decoding.

Use Schema helpers for untrusted JSON; no any, star/alias imports, globalThis, sleeps or type assertions hiding shape errors. Source reader SQL is parameterized by selected Session IDs. Return owned files/guarantees and explicit unsupported cases, no command/test claims.
