# Worker 2 of 3 — receiver lifecycle and scope hardening

Model opencode-go/deepseek-v4.1-flash/high. Own ONLY src/transfer-receiver.ts and test/transfer-receiver.test.ts under D:/OpencodeHarness/modular/packages/adapters-opencode. Attached snapshots are complete context. No exploration, commands/tests, Git, delegation or other edits. Public constructor/return/wire shapes MUST remain unchanged so worker1 can test them independently. Return changes and uncertainties; no test-pass claims.

Current code passes existing tests but master review identified these specific gaps. Fix them with focused real-spool/SQLite tests rather than rewriting the working receiver.

1. **Scope collision before authorization:** spool.begin uses workspace+clientTransferID, so different receiver scopes sharing a spool can collide before cm_transfer_binding rejects them, potentially touching a foreign TTL. Namespace the INTERNAL spool clientTransferID by frozen trusted scope and receiver instance identity, while preserving the external request/begin_json identity. All other operations still require an admitted handle and exact scope. Do not alter transfer-spool.ts. Test that a foreign receiver cannot change/reuse another scope's active transfer via same clientTransferID/metadata; the original remains finalizable.
2. **Metadata lifecycle bounds:** successful finalizations/aborts currently retain entire Binding/rawBegin maps until absolute expiry, even though spool resources are gone. Release those active in-memory entries promptly. Keep exact begin/finalize/abort retries correct through bounded durable metadata (cm_transfer_receipt/binding), with immutable external begin_json identity and expiry. Recreated receivers with identical scope must recover committed begin/finalize; a changed external begin payload using the same client ID conflicts. An abort must not erase a committed receipt. Unknown/foreign handles remain forbidden; expired handles/receipts must not become reusable. Keep completed metadata bounded by TTL, and active entries by actual spool limits; no second transcript/Session table.
3. **Cancellation/orphan cleanup:** a canceled begin after spool creation but before control binding insertion must not orphan bytes/keys or later hand a handle to the wrong scope. Use proper Effect uninterruptibleMask/restore or scoped cleanup. Failed append/precommit finalize must release active maps consistently with spool invalidation. A committed receipt remains the authority if postcommit cleanup is interrupted/fails; retry should finish cleanup without replay. Dispose all owned resources after pending operations settle.
4. **Durable commit fences:** retain authorization+absolute/idle expiry rechecks immediately before receipt insert and after awaits, and final placement/owner checks after replay. Test expiry/authority change DURING a restore, not just between begin/finalize. Always rollback every aggregate/notification on precommit failure. Ensure raw begin metadata is detached before await (already structuredClone replay request); don't regress it.
5. Register receiver control tables under the shared cm_migration ledger if missing, using a unique id such as 0006-transfer-control, while leaving native migrations untouched. Parent kernel already creates cm_migration; constructor may CREATE IF NOT EXISTS for safe independent bootstrap. No schema/key changes to existing committed receipt fields that would break recovery without an explicit migration.

Use real SessionContextTransferSpool and native database/EventBoundary, not mocks/globalThis. Existing tests and native event/context fixture helpers are attached. Add deterministic Deferred-based cancellation/expiry/concurrency cases; no sleeps or timed races. If you need exact pinned APIs: Effect.uninterruptibleMask(restore=>...), Effect.tryPromise({try,catch}), Effect.onExit, Effect.catch, Effect.forkScoped({startImmediately:true}), Fiber.interrupt returns void, Fiber.await returns Exit, fiber.pollUnsafe() returns Exit|undefined. No Effect.fork, zipRight, catchAll or Cause.failureOption. No any, star/aliased imports, unchecked casts or new native patches.
