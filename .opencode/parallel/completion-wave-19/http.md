# Worker — actor-bound CtxPack catalog HTTP

Model opencode-go/deepseek-v4.1-flash/high. Own ONLY modular/packages/adapters-opencode/src/ctxpack-http.ts and test/ctxpack-http.test.ts. Supplied source snapshots contain all dependency APIs. Do not explore, run commands/tests, Git, delegate, touch other files, or import fork runtime. Create code promptly, report uncertainties. Master binds/test after return.

Export createCtxPackHttp(input:{readonly catalog:CtxPackCatalog;readonly materializer:CtxPackMaterializer;readonly authenticate:(request:Request)=>Promise<CtxPackActor|undefined>;readonly run:<A,E>(effect:Effect.Effect<A,E>)=>Promise<A>}) returning {fetch:(request:Request)=>Promise<Response|undefined>}; borrowed instances, no independent runtime/database/disposal. Own EXACT `/api/cybermastery/ctxpack` prefix: other paths return undefined; unknown paths under prefix 404 and MUST NOT fall through to native fallback. Auth before body read, require Authorization HEADER for private bodies (query auth_token alone rejected) and trusted callback to supply actor. No actor/user/workspace from request body/query can override authenticated actor. Return `Cache-Control:no-store` on all responses.

Frozen endpoints: POST `/` create using catalog.create(actor,body); POST `/list` catalog.list(actor,body) (full frozen CtxPackListRequest with all required fields incl limit/sort/nullable filters); GET `/:id` catalog.get(actor,id,includeDeleted from `?includeDeleted=true` only); PATCH `/:id` catalog.patch(actor,body) with path ID required equal body.ctxPackID, path actor workspace required equal body.workspaceID; DELETE `/:id` expects JSON `{expectedRevision:number}`, catalog.remove; POST `/:id/restore` same; POST `/:id/pin` and `/unpin` require empty JSON `{}` and map to catalog.pin/unpin (unpin 204); POST `/:id/materialize` `{expectedContentHash,targetInstanceID,targetFunctionalityID}` and inject actor.workspaceID + id as request; no capsule body provided by client. Unsupported methods 405 Allow with actual supported verb. /list is never confused with :id. No endpoint sends fragment bodies except authenticated create/get/patch/remove/restore; list returns summary only. Materialize returns ID/label/hash/tokens only.

Strict JSON via Schema.UnknownFromJsonString and Schema.decodeUnknownOption(...,onExcessProperty:'error'); actual streamed body cap1MiB including unknown/missing content length; oversize413; wrong MIME415; malformed400. Inspect before returning any input to client; prevent private text in errors. Map CtxPackError union to stable sanitized public `{code,message}`: NotFound404, Deleted410, RevisionConflict409 (include currentRevision integer), ContentChanged409 (include currentContentHash string), InvalidSelection/Secret/CrossWorkspace/SearchCursor400, PermissionDenied403, BudgetExceeded413; MaterializeError CapsuleMissing404/Expired410/TargetMismatch403/SnapshotDuplicate400/SnapshotOverBudget413/CapabilityDenied403. Unknown failure/defect500 fixed text. Preserve interrupt (do not turn it into 500 inside run effect; outer Request disconnect handled by fetch). Never echo error.reason or fragment body. Validate IDs ctxpk_ for path. Dispose/close request readers; no private values in logging. Use existing `session-http.ts`/`transfer-http.ts` route/error patterns, without replacing native HTTP or editing Protocol/Server HttpApi.

Tests with REAL catalog/materializer/capsule same native DB via createMediatedFixtures; per-file databaseCleanup; use function auth actor port (no fake store). Verify unauthorized/wrong workspace and query-only rejection BEFORE body read, create/GET/list/patch/pin/remove/restore/materialize end-to-end, source tags/provenance preserved, exact create idempotency, revision conflict, private other actor filtered from list/count and GET, no private bytes in error responses, malicious unknown fields/oversize, unknown owned route and method cannot fall through, time-unavailable capsule, materialization target mismatch. Test actual integration borrow handles without creating second runtime; master tests combined app later. No any/non-null assertions/globalThis/sleeps.
