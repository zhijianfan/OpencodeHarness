# Worker — CtxPack browser-safe extension client

Model opencode-go/deepseek-v4.1-flash/high. Own ONLY modular/packages/client/src/ctxpack.ts and ctxpack.test.ts. Attached contract/server routes/existing client supply context. No exploration, commands/tests/Git/delegation or other edits. Produce implementation promptly, return uncertainties. Master integrates/tests after return.

Export createCtxPackClient(options:{readonly baseUrl:string;readonly token:string;readonly workspaceID:string;readonly fetch?:typeof fetch}) with typed Promise methods create(request:CtxPackCreateRequest,signal?), get(ctxPackID:string,includeDeleted?:boolean,signal?), list(request:CtxPackListRequest,signal?), patch(request:CtxPackPatchRequest,signal?), remove(ctxPackID:string,expectedRevision:number,signal?), restore(same), pin(ctxPackID,signal?) returning CtxPackSummary (server intentionally strips fragment bodies), unpin(ctxPackID,signal?) returning void, materialize(ctxPackID:string, input:{expectedContentHash:string;targetInstanceID:string;targetFunctionalityID:string},signal?) returning CtxPackMaterializeResult. Export CtxPackClientError extends Error with readonly status/code/currentRevision?/currentContentHash?; error messages MUST NOT include raw server text/fragment bodies.

Runtime package `@cybermastery/client` depends only on `@cybermastery/contracts`, not Core/Protocol/Server. Follow client/src/client.ts style for base URL/auth/abort; do not import Effect/native modules, browser-compatible fetch/URL/JSON only. Root `POST /api/cybermastery/ctxpack/` create, `POST .../list`, GET/PATCH/DELETE `/:id`, POST `/:id/{restore,pin,unpin,materialize}`. Every request explicitly Bearer token in Authorization header (never query), JSON Content-Type for bodies, `cache:'no-store'`, signal forwarded, `redirect:'manual'` and reject 3xx without forwarding. Require nonempty configured token/workspace; request.workspaceID must equal configured workspace BEFORE sending; validate ID prefix and revision safe integer locally. Encode path ID with encodeURIComponent. Pin response is CtxPackSummary without fragment bodies, unpin expects 204 empty body. Error responses sanitized/parsed only from known `{code,currentRevision?,currentContentHash?}` fields with no raw reason; status-specific fallback. No blob/text in errors or logs. Never synthesize fake pack/fragment data. Success response structurally validate IDs/workspace/revision/fragment arrays/tags/hash/count and status-specific shapes before returning typed CtxPackInfo/Summary/ListResult/MaterializeResult; strict reject missing/unknown discriminator or wrong workspace/capsule id. Detailed schema source in attached contract. `fetch` default global fetch; optional injected fetch port useful for real/integration tests, avoid mocks for storage. Dispose? client has no resources.

Tests: use real Bun.serve ephemeral HTTP responder recording requests (not duplicating catalog implementation) plus explicit malicious/non-JSON/error outputs for validation. Verify every verb/path, auth/no-store/redirect policy, actor workspace preflight, path/abort handling, no fragment leakage in errors, pin strips fragments, materialize only ID/label/hash and expiry/error payload handling. Integration with real application adapter is master's separate test. Tests run from modular/packages/client via `bun typecheck && bun test src` AFTER worker return. No any/non-null assertions/star/aliased imports/globalThis/sleeps.
