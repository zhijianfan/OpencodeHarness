# Worker 1 — parent tool and child ownership proof

Model opencode-go/deepseek-v4.1-flash/high. Own ONLY D:/OpencodeHarness/modular/packages/adapters-opencode/test/parent-tool-child.test.ts (new). All context attached. No exploration, commands/tests, Git, delegation or other edits. Return guarantees/uncertainties without claiming tests run.

Close the executable seam test identified by plan sections6/7: a child started by an ACTUAL parent provider tool races external child resume/interrupt and parent cancellation, through one native coordinator. This is a proof of the boundary task_batch will compose, NOT extraction of its manifests/CtxPack capture/archival. Name the fixture tool delegation_probe and don't claim it implements task_batch.

Use createSessionRuntime with actual native Session/Event/Store/LocationServiceMap and deterministic LLM replacements from child-runner.test.ts; native branded IDs/Agent/Provider/Model refs. Capture makeChildRunner at the root with REQUIRED explicit fixture actor and parent placement authorization. Resolve the parent's actual ToolRegistry via LocationServiceMap.Service: `const locations=yield* LocationServiceMap.Service; const registry=yield* Effect.provide(ToolRegistry.Service, locations.get(parent.location))`. Keep registry.register inside Effect.scoped for lifetime. Registration returns Effect<void,RegistrationError,Scope.Scope>. It is the SAME Location registry used by the owned native runner; do not override SessionRunner or call its run method directly.

Register Tool.make({description,input:Schema.Struct(...),output:Schema.Struct(...),toModelOutput:({output})=>[{type:'text',text:output.text}],execute:()=>child.run(frozenInput)...}). Convert typed ChildRunError to actual Tool.Failure if needed; never swallow interruption to leave an owned child running. Parent LLM fake first emits LLMEvent.toolCall({id:'delegation',name:'delegation_probe',input:{...}}) plus stepFinish reason tool-calls; next parent turn returns normal text. Child LLM is keyed by request.http headers X-Session-Id and held on Deferred. Responses/signatures are attached; no ModelV2.ProviderID (use ProviderV2.ID).

Tests:
1. Admit parent, resume it through SessionExecution; actual tool starts child; external child SessionExecution.resume joins it, provider requests show ONE child turn; explicit interrupt(childID) settles both child tool and external waiter. Parent/tool failure mapping must be asserted (actual cleanup), not a timeout accepted as success.
2. Interrupt(parentID) while the tool-initiated child owns its pending drain: wait for parent/tool cleanup, assert the child owner is gone and child provider finalizer ran; unrelated Session/child remains unaffected if practical.
3. Tool joins a child already owned by an independent pending caller with exactly matching parent/config/prompt identity: parent interruption must cancel only its joining waiter, not hijack and interrupt the pre-existing child's owner. Release that provider and prove original caller completes without another child turn.
4. Exact completed supplied child/prompt retry from a subsequent tool call does not freeze or call the provider again. Stable child title/agent/model/variant/parent checks remain actual child API behavior.

Use Deferred barriers, scoped fibers, immediate starts and explicit cleanup of native execution owners on assertion failure. No sleeps/timed races/globalThis/any/aliases/star imports or guessed APIs. Effect beta.83 has forkScoped (not fork), Fiber.await, Fiber.interrupt returning void, fiber.pollUnsafe(), Cause.findErrorOption and Cause.hasInterruptsOnly. Include finite overall test timeouts as failure bounds, not coordination.
