# Worker — trusted OperatingChat binding profile

Model opencode-go/deepseek-v4.1-flash/high. Own ONLY modular/packages/adapters-opencode/src/operating-chat-binding.ts and test/operating-chat-binding.test.ts. Full relevant source/contract attached. No exploration, commands/tests, Git/delegation/other edits. Produce code promptly and report uncertainties. Never import fork OperatingChat engines at runtime or patch vendor.

Frozen contract @cybermastery/contracts/operating-chat: OperatingChatDescriptor {workspaceID,workspaceName,blockID,functionalityID:'builtin:operating-chat-session',functionalityInstanceID,directory,operatingAgent}, OperatingChatProfile {kind:'operating-chat', all descriptor fields, generation,revision}, OperatingChatBindingError {_tag:'OperatingChatBinding.Error',code:'unauthorized'|'invalid'|'conflict'|'stale'|'missing-session'}. Real native SessionSchema.ID, SessionV2.Service, Database.Service and EventBoundary are attached. This is a TRUSTED selected-host binding adapter, not the entire fork's workspace/FunctionalityInstance implementation.

Export makeOperatingChatBinding(options:{readonly authorize:(input:{readonly actor:{readonly userID:string;readonly workspaceID:string};readonly action:'bind'|'read'|'reset';readonly descriptor?:OperatingChatDescriptor;readonly sessionID?:SessionSchema.ID})=>Effect.Effect<void,OperatingChatBindingError>}). Effect constructor captures Database.Service, SessionV2.Service and EventBoundary. Return {
 bind:(input:{actor:{userID:string;workspaceID:string};descriptor:OperatingChatDescriptor;sessionID:SessionSchema.ID;expectedRevision?:number})=>Effect.Effect<OperatingChatProfile,OperatingChatBindingError>;
 resolve:(sessionID:SessionSchema.ID)=>Effect.Effect<OperatingChatProfile|undefined,OperatingChatBindingError>;
 revalidate:(sessionID:SessionSchema.ID,profile:OperatingChatProfile)=>Effect.Effect<void,OperatingChatBindingError>;
 reset:(input:{actor:{userID:string;workspaceID:string};sessionID:SessionSchema.ID;expectedRevision:number})=>Effect.Effect<void,OperatingChatBindingError>;
 }. Methods captured, no further environment. bind first revision1/generation1; later rebind requires expectedRevision exact, increments generation+revision with CAS, different Session binding retains previous event/transcript; `reset` revokes profile (soft deleted) only after exact revision. Do not create/execute/delete Sessions or mutate native workspace/functionality tables; no fake feature registration. Caller authorization port is REQUIRED to validate descriptor functionality instance/block/actor against its observed layout/workspace owner at both pre-read and commit. Adapter independently enforces nonempty actor/ids, descriptor.workspaceID==actor.workspaceID, actual native session recorded location.directory and workspaceID matching descriptor and session classified v2 (cm_session_runtime exactly v2; imported historical legacy/mixed fail closed). Use native SessionSchema.ID brands, no plain values for Location.workspace identity. Double-bound Session/duplicate live (workspace,block) conflict; begin with actual recorded session (no invented placement). Session reuse adoption must not reclassify. Table cm_operating_chat_binding(workspace_id,block_id,instance_id,session_id,directory,workspace_name,operating_agent,generation,revision,deleted_at, PRIMARY KEY(workspace_id,block_id), unique active session_id) in the SAME selected native database, register cm_migration '0010-operating-chat-binding' without touching native ledger. Use EventBoundary.transaction for every write; reauthorize/fresh-read inside transaction, SQL CAS across independent DB connections, no emitted untrusted body. resolve queries ACTIVE rows by session_id and verifies actual native Session location+runtime before returning; corrupt/missing record fails `stale` (not generic fallback). revalidate compares all profile fields against a fresh resolve (including workspaceName,operatingAgent,instanceID,generation/revision/directory), protects admission from profile changes. Catalog recall later consumes `resolve`; no standalone automatic recall for generic Sessions. SQL/driver failures sanitized defects without private body.

Tests: real createSessionRuntime with workspace branded ID and native Session.create/project owner, same file DB, databaseCleanup/disposal. Confirm bind, read, revalidate, reset; cross-workspace/foreign directory/unknown/legacy/mixed Session reject with no writes; owner revocation between first check/commit rejects; duplicate live session to two blocks, same block CAS competition on two connections, stale generation/revision after rebinding/reset, surviving Session transcript unaffected and native migration ledger unchanged. No mocks of DB/service, no globalThis/sleeps/any/star/aliases/unchecked casts. This is a bounded profile proof, full OperatingChat ensure/reset UI follows later.
