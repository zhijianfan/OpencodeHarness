# Worker — OperatingChat CtxPack bounded recall

Model opencode-go/deepseek-v4.1-flash/high. Own ONLY modular/packages/adapters-opencode/src/ctxpack-recall.ts and test/ctxpack-recall.test.ts. All relevant sources attached; no exploration, commands/tests, Git, delegation, native/fork imports or other edits. Implement then report source-backed uncertainties, not test-pass claims. Do not change app/Session facade; master later wires ONLY to a trusted OperatingChat binding.

Export makeCtxPackRecall(options:{readonly catalog:CtxPackCatalog;readonly authorize:(input:{readonly actor:CtxPackActor;readonly operation:"ctxpack.read"|"chat.context.attach";readonly pack?:CtxPackInfo;readonly target?:{readonly workspaceID:string;readonly instanceID:string;readonly functionalityID:string}})=>Effect.Effect<void,CtxPackError>}) as Effect constructor capturing Database.Service; return {select:(input:{readonly actor:CtxPackActor;readonly target:{readonly workspaceID:string;readonly instanceID:string;readonly functionalityID:string};readonly promptText:string;readonly explicit:readonly ContextSidecarAttachment[];readonly budget:{readonly maximumBytes:number;readonly maximumEstimatedTokens:number}})=>Effect.Effect<{readonly attachments:readonly ContextSidecarAttachment[];readonly status:"skipped-trivial"|"no-match"|"selected"|"unavailable"}>}. The `explicit` array is already validated+materialized from capsule, never a client body. Do not automatically mutate Session, create new capsules or count usage; selection remains advisory until native prompt admission freezes and commits snapshot. Return automatic only, caller renders explicit first. Capture DB once; returned method has no further requirements.

Baseline references core/ctxpack/{recall,session-context}.ts and new domain buildRecallTerms/isTrivialRecallTurn + owned renderContextSnapshot. Preserve: trivial normalized turn skip search; terms NFKC/lowercase stopwords stable unique max8; search via parameterized FTS5 MATCH of quoted OR terms, rank bm25 ASC then pack ID ASC, max16 CANDIDATES before authorization. Query cm_ctx_pack_fts JOIN cm_ctx_pack workspace exact, not deleted; PRIVATE packs filtered for noncreator before LIMIT/ranking (never leak private candidate IDs/hash). Some candidates may fail read/target policy; skip without leaking data; interruption propagates, database/search failure becomes unavailable (no private text in error/log). Ensure search doesn't cross workspace even if malicious stored FTS rows. Target.workspaceID must equal actor.workspaceID before search; require chat.context.attach at target before any candidate. catalog.get(actor,id,true) must recheck current hash and deletion and permission for every candidate. Do not trust only candidate FTS row. Automatic cap4, combined explicit+automatic cap8; skip duplicate sourceCtxPackID + contentHash already explicit or selected. Candidate with current fragments over budget is skipped via actual renderContextSnapshot of [...explicit,...selected,candidate] with recall policy operating-chat-v1 status selected at createdAt0, under caller budget. Automatic attachment includes selection automatic, pack id/label/tags/hash and ordered fragment text/contentHash only; no capsule id or source-provenance text in public DTO. Do not let one oversized candidate discard valid later candidates. Catalog returns pack with real current fragments; target permission must be revalidated before return; avoid exposing private body in error/diagnostics. Empty terms=>no-match; total search error=>unavailable; all candidate denials=>no-match. All input fields detached at operation admission; permission inputs use only authenticated actor and trusted target.

Tests real file-backed mediated runtime + actual CtxPackCatalog/FTS; create public/workspace/private packs with relevant terms, deterministic stable rank tie breaker if possible, code-point/trivial cases, cap16 search and cap4 automatic, explicit priority/max8, contenthash duplicate skip, inaccessible/deleted/changed pack skip, too-big first candidate then smaller later, wrong workspace before DB disclosure, malformed term FTS fails closed, interrupted candidate propagates with no subsequent evaluation; candidate labels/fragment text never appear in error output. No mocks of catalog/storage, no globalThis/sleeps, per-file databaseCleanup/disposal. Tests may use function-injected authorizer policy, not fake pack contents. Effect v4 APIs from attached sources; no any/star/alias imports, no unchecked casts.
