# Worker 1 of 2 — bounded source history transport

Model opencode-go/deepseek-v4.1-flash/high. Implement only src/transfer-source.ts and test/transfer-source.test.ts under D:/OpencodeHarness/modular/packages/adapters-opencode. All context is attached; no exploration, commands/tests, Git, delegation, vendor or other edits. Return files changed and uncertainties, never a test-pass claim. Keep reasoning bounded and produce the implementation.

## API (frozen shared protocol is attached)

Export makeTransferSource(options: TransferPolicy & {readonly now?:()=>number;readonly maxSnapshots?:number;readonly maxSnapshotBytes?:number;readonly maxTotalSnapshotBytes?:number}). It is an Effect constructor capturing Database.Service, EventBoundary and any EventV2.Service dependency needed by the actual projection. Return {history:(payload:unknown)=>Effect.Effect<TransferHistoryResponse,TransferError>, required:Effect.Effect<boolean,TransferError>, dispose:Effect.Effect<void>}. All returned methods must capture/provide native dependencies so callers only need the constructor's environment once. TransferPolicy scope is trusted, detached/frozen at construction; require nonempty principalID/ownerID/projectID/directory and nonempty workspaceID when supplied. Mandatory authorize(scope,'history') before disclosure and again inside each snapshot transaction. No second graph, database, process-global cache/key or runtime-import of fork modules.

## Behavior/source evidence

Attached fork sync handler is CUSTOM APPLICATION reference code, not a native engine to copy. Port its source-history behavior over existing makeLegacyProjection, EventBoundary.transaction and native database. Use attached transfer-protocol exports for request/response/error/TransferPolicy; do not redeclare or edit the shared module.

Decode unknown payload strictly with TransferHistoryRequest and onExcessProperty:error; detach it BEFORE any await using Schema or canonical JSON. All aggregate IDs must be native SessionSchema.ID-compatible. At most 128 requested/discovered aggregates. Discovery queries native session rows for EXACT trusted project_id and workspace_id (NULL for implicit-local) ordered by id with limit129 and cursor; additionally ensure existing Session directory matches trusted directory before export. Explicit requested foreign/missing Sessions fail forbidden instead of silently skipping. Source owner is options.scope.ownerID, not an untrusted payload. makeLegacyProjection.authorize must translate the trusted authorization result, and its export receives sessionID/workspaceID/ownerID.

Snapshots: instance-local map and random cursor HMAC key, default 8 active snapshots, 512MiB/snapshot, 1GiB total, absolute 5-minute TTL (no extension by retries). Count canonical frozen payload bytes; clean expired entries before admission and fail busy/too-large instead of unbounded storage. Frozen highWater, canonical bundles and record list are computed in ONE EventBoundary.transaction (nested projection exports supported; never wrap a raw SQL transaction outside it). Binding includes principal/owner/project/workspace/directory, aggregates, discoveryCursor and repairCursor; capabilityOnly can change when continuing the same snapshot. pageCursor requires a valid sourceSnapshotToken. Token and signed cursor cannot be reused across source instances/scopes/requests. Validate cursor position is actually within snapshot bounds, not merely nonnegative. Return detached results so caller mutation cannot corrupt caches. dispose clears private payloads and key, and later requests fail closed.

Each continuation reauthorizes placement/owner, and re-exports under the boundary to validate frozen history below highWater. Preserve fork normalizedBundle behavior: append-only events above highWater are allowed; later proven reverts can use frozen original contexts; later epochs can retain frozen baseline; mutation below highWater fails snapshot-advanced. Do not accept receiver cursors ahead of the source. If snapshot expiration occurs while blocked on authorization/export, recheck before disclosure. required examines cm_private_requirement/input/checkpoint and native context epoch for the bound project/workspace without exposing bodies, with authorization; include unsupported proof-only private rows rather than ignoring them.

## Full-private-snapshot overlay (intentional boundary adaptation)

Unlike the old handler's needsPrivate optimization, every DATA snapshot includes the COMPLETE contexts/deletions/epoch set for its selected aggregates even when events are an incremental suffix. This is necessary because the current legacy projection verifies a complete private manifest, including retained history. Do not optimize away those records based on cursor.privateDigest. Capability-only replies expose manifests/highWater and an empty page, never private bodies. repairCursor still selects a single discovered aggregate when no aggregates were explicitly requested and must refer to an authorized discovered ID. manifestDigest must describe exactly the transferred private sets (filter empty manifests as the existing spool does).

## Page wire

Reuse spool canonical/digest/privateManifest/manifestDigest and constants. Do NOT use legacyCanonical for page hashes (different canonical ordering). Records are ordered globally: non-epochs first; aggregateID.localeCompare, sequence, kind rank(event=0,deletion=1,context=2), identity; all epochs last. Record identities follow existing spool validateRecord. Public events only above destination cursor; complete private records retain original sequence. Enforce the WHOLE canonical response (not just page) <=512KiB, <=256 event/deletion records per page, <=64 chunks, and chunk-only pages. Oversized record values split canonical UTF-8 bytes into 32KiB parts, canonical Base64 with full byteLength/contentHash and immutable chunk identity. Do not mix chunk/complete records. Preserve signed nextCursor and deterministic page retries. Empty dataset has an empty page and no nextCursor; no stalled nonempty page.

## Tests

Use real native services/temp SQLite via attached createMediatedFixtures/createSessionRuntime patterns, supplied file cleanup, and disposable source instances. Tests: authorized discovery and foreign scope rejection; page retry equality and caller-mutation isolation; request/token/cursor mismatch and cross-instance use; >256 events pagination; >512KiB context/event chunking with real SessionContextTransferSpool roundtrip; capability-only body exclusion; normal append above frozen high-water succeeds; altered earlier private/event data rejects; expiry after delayed authorization; instance limits/disposal. Actual legacy source fixtures need native durable event shapes, canonical sidecar relationships and explicit owner_id claim; copied fixture builders may be adapted in your test only. No fake native API guesses, sleeps, globalThis, any, aliased/star imports or assertions masking type errors.
