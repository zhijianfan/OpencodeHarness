# Worker 2 of 2 — authenticated atomic transfer receiver

Model opencode-go/deepseek-v4.1-flash/high. Implement only src/transfer-receiver.ts and test/transfer-receiver.test.ts under D:/OpencodeHarness/modular/packages/adapters-opencode. All context attached; no exploration, commands/tests, Git, delegation, vendor or other edits. Return changed files/uncertainties, never test-pass claims. Keep reasoning bounded and produce the implementation.

## API

Export makeTransferReceiver(options: TransferPolicy & {readonly spool:SessionContextTransferSpool;readonly now?:()=>number}). It is an Effect constructor capturing Database.Service, EventV2.Service and EventBoundary; initializes its own extension receipt ledger in the SAME database (no native migration edits). Return {replay:(payload:unknown)=>Effect.Effect<TransferReplayResponse,TransferError>,dispose:Effect.Effect<void,TransferError>}. The returned methods capture/provide required native services; no caller requirements after construction. The receiver owns the passed dedicated spool's disposal, no second graph/database. Use only the attached shared transfer-protocol exports for DTOs/errors/policy. Trusted scope is detached at construction and requires nonempty principalID/ownerID/projectID/directory (workspace nonempty if supplied).

## Scope and ingress

Decode/detach unknown payload strictly with TransferReplayRequest (onExcessProperty:error) before the first async boundary. Authorize(scope,'replay') before each operation, including retries/abort/receipt reads, and again inside final commit. No principal/project/workspace/owner comes from the body. Each receiver instance is bound to its own scope and retains a handle->binding map; tokens from a different receiver/scope must not work even when sharing a spool reference (tests may instantiate with distinct scopes). Bind begin identity, highWater, snapshot token, directory, manifest, expiry and principal/owner/project/workspace. Validate directory exactly and <=128 highWater Session IDs before spool mutation. Existing targets must match trusted project_id, directory, workspace_id and cannot have a different non-null event_sequence.owner_id. Missing Sessions are allowed only when the eventual incoming Created event matches the same placement. Never steal/overwrite owner claims.

Native tables/project/event existence requirements are real: new Session restore requires an already provisioned matching project. Reject missing project; don't invent IDs or auto-import a foreign project. Native Event.replayAll(strictOwner:true) claims an unset owner atomically; do NOT preemptively call events.claim to overwrite ownership. makeLegacyProjection already checks expected owner/workspace; additionally validate project/directory/Created relationship inside outer commit. Event table stores JSON data. Use adaptLegacyEvent for native Created lookup or SessionV1.Event.Created versioned type.

## Spool and commit

Use actual SessionContextTransferSpool.begin/append/finalize/abort/complete with the attached exact signatures; map known exceptions to TransferError busy/too-large/expired/conflict and sanitize unknown causes. Bind every action to an admitted handle; begin retries must remain immutable. Serialize receiver operations that share a handle, including finalization+abort, with Effect Semaphore, not a second Session execution owner. Respect interruption; no native mutation before finalize validates every page/hash/manifest.

Finalize: obtain ready bundles, then restore ALL aggregates serially within ONE EventBoundary.transaction. Revalidate authorization, every placement/owner and expiry at commit. Normalize optional undefined epoch from spool before strict legacyCanonical processing. Use each authenticated spooled bundle's detached canonical digest as expectedDigest for makeLegacyProjection.restore, scoped owner/workspace, publish:true; this is an internal bridge over already hash/manifest-validated payload, not accepting a client-supplied digest bypass. Current projection requires complete private contexts/deletions/epoch; source worker includes them even for incremental event suffixes. Tests can construct pages directly from real bundles.

Persist a durable cm_transfer_receipt row INSIDE that same transaction, binding handle, canonical trusted scope/begin identity, expectedPageCount, manifestDigest, receipt and expiresAt. Receipt exists only after ALL aggregates commit; notifications are buffered by the boundary. Do not call spool.complete or delete bytes inside SQL transaction before commit. After commit, clean the spool and return manifestReceipt. If cleanup fails/interruption occurs after commit, a retry must recover via the durable ledger, not re-run projection or fabricate a new receipt. Receipt retries must verify scope, manifest and page count; work across receiver recreation with the SAME trusted scope even if the in-memory spool is gone. Expired receipts are not reusable. The extension ledger is control metadata, never another transcript authority.

Record expiry at begin as min(source expiry, now+30min); enforce both absolute expiry and 5min idle budget around awaits. A finalize that crossed expiry while waiting on authorization/transaction must not commit. Do not allow abort of a committed receipt to erase a valid durable acknowledgement. Failed precommit restore rolls back all aggregates/metadata/notifications and disposes the failed spool payload; no partial prefix acknowledgment. No provider wakes on replay. Strict same-handle finalize retries return the same receipt without duplicate native events. Keep map/ledger cleanup bounded by expiry; disposed receiver rejects all calls.

## Tests

Real temporary native SQLite + EventBoundary + actual spool, using attached legacy-projection fixture builders/createMediatedFixtures. Verify begin/append/finalize happy path and exact retry, data-only spool before commit, multi-aggregate rollback on later conflict and zero notifications, owner/project/workspace/directory rejection, authorization revoked between begin/finalize and during commit, expiry while authorization is delayed, missing pages/manifests, scope/handle replay, concurrent finalize/abort, receipt binding and recovery after receiver recreation without spool data, no public private-body leakage, cleanup/disposal. Build actual native event/context bundles and use canonical(page) hashes, not a fake restore implementation. No mocks/globalThis/sleeps/any/star/alias imports or unsafe casts. Test cleanup must dispose runtime/spool and use databaseCleanup for SQLite Windows files. Effect beta.83 APIs are shown in attached modules; Effect.catch (not catchAll), Cause.findErrorOption (not failureOption), Fiber.interrupt returns void.
